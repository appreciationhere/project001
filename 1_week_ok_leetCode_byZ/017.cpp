#include <bits/stdc++.h>
using namespace std;
int waysToReachTarget(int target, vector<vector<int>>& types);

int main() {
	
	return 0;
}
int waysToReachTarget(int target, vector<vector<int>>& types) {
        
}
int dfs(int t, int i, vector<vector<int>>& types) {
	if (i == n)
		return 0;
	if (t == k)
		return 1;
	for (int j = i + 1; j < n; ++j) {
		
	}
}